{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "//": "자료(public/data)만 바뀐 커밋에서는 다시 짓지 않는다. 화면은 자료를 굽지 않고 실행 중에 가져가므로 다시 지을 이유가 없다. 종료코드 0이면 버셀이 빌드를 건너뛴다.",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- . ':(exclude)public/data'"
}
